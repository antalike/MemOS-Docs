# Copy to `.env.local` to override any profile locally (git-ignored).
# See also `.env.development`, `.env.production`, and `.env.pre`.

NEXT_PUBLIC_APP_ENV=prod
NEXT_PUBLIC_SITE_URL=https://memos-docs.openmem.net
NEXT_PUBLIC_API_BASE=https://apigw.memtensor.cn
NEXT_PUBLIC_DASHBOARD_API_BASE=https://memos.memtensor.cn
NEXT_PUBLIC_DASHBOARD_URL=https://memos-dashboard.openmem.net
NEXT_PUBLIC_AUTH_COOKIE_NAME=memos_token
