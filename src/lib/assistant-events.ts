// Window event used to toggle the global AI assistant panel from anywhere in the
// app (e.g. the home navbar "Ask AI" button). The `Assistant` overlay is
// mounted once at the root and listens for this event.
export const OPEN_ASSISTANT_EVENT = 'memos:open-assistant';
